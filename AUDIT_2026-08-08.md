# Delivery Audit - 2026-08-08

## Confirmed in Code and Tests

- Main `CSI` extraction is explicit; `LegacyCSI` is rejected as model input.
- Node1/Node3 matching uses `(source_mac, sequence, fragment, task_id)`. In the supplied captures, all 5,556 Node1 keys and all 5,658 Node3 keys were unique.
- Robust Node3-to-Node1 offset/drift fitting produces corrected timestamps and residuals.
- Format-specific model preparation produces 56 tones for HT20 and 234 data tones for the supplied HE-SU20 capture and the VHT80 profile.
- Strict validation checks required arrays, row counts, monotonic indices/timestamps, main CSI, captured packet format, captured bandwidth, expected subcarrier count, finite CSI values, and camera quality when enabled.
- Radio configuration, traffic mode, transfer byte checks, unique trial directories, and batch failure summaries are connected to the orchestration path.
- RGB-D output is documented as synchronized reference data, not 3D pose ground truth.

## Verification Evidence

- Python unit/contract tests: 30/30 passed.
- Python compilation: passed for all five Python entry points.
- Bash syntax: passed for `csi_lab.sh`, `csi_agent.sh`, and `field.sh`.
- All four supplied profiles load successfully.
- Existing HE-SU20 integration data: 5,386 matched packet pairs; model input `(5386, 234, 2, 1)` per receiver; corrected timing residual p95 `84,097 ns`; strict validation passed as `HESU / 20 MHz`.

## Not Yet Proven or Implemented

- HT20, VHT20, VHT80, broadcast, multicast, and camera paths still require real-node pilot captures.
- The package does not generate 3D joint labels or perform camera-to-Wi-Fi spatial calibration.
- RSSI extraction and CSI/RSSI feature-comparison experiments are outside this package and still need a separate analysis stage.
- OAK-D versus Azure Kinect suitability requires supervisor approval.
