# `leg_lift_05m_pilot_02` 质量评估

## 结论

**不计入固定布局数据集。原始数据保留。** 动作本身执行正确、数据完整，但 RGB 明确显示左侧木门在本 trial 开始前已经打开；正式空场基线与 `pilot_01` 中该门关闭。门板是大型反射体，这一变化会改变 Wi-Fi 多径，不能作为同一布局的独立重复。

## 完整性

- node1/node3 目标包 6933/6940，匹配 6884；匹配率 99.29%/99.19%。
- 234 个数据子载波，无非单调包。
- OAK-D-W 记录 1501 帧深度，30 FPS，序号缺口 0；H.265 可解码。
- 对齐器提示重复包键按接收顺序配对；输出时间仍单调且匹配率正常，因此这不是排除本 trial 的主要原因。

## 信号记录（不用于同布局倍率结论）

以关门状态的 `empty_05m_pilot_02` 为参照时：

| 指标 | node1 | node3 |
|---|---:|---:|
| RSSI 标准差倍率 | 0.89× | 1.70× |
| 100 ms CSI 变化倍率，天线 0 | 2.56× | 1.80× |
| 100 ms CSI 变化倍率，天线 1 | 1.63× | 1.63× |

由于基线与 trial 门状态不同，这些数值仅供排查，不能与 `pilot_01` 直接合并。
